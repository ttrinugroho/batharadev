import { HotRouteOptions } from './interfaces';
export declare class Router {
    opts?: HotRouteOptions;
    enabled: boolean;
    constructor(opts?: HotRouteOptions);
    go(path: string): Promise<void>;
    disable(): void;
    private prefetch;
    private onClick;
    private onPop;
    private replaceDOM;
}
